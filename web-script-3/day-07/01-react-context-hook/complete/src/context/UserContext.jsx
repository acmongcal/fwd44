import { createContext } from "react";

export const UserContext = createContext();

// Scaled up with useReducer
// export const initialState = {
//   user: null,
// };

// export const UserContext = createContext(initialState);
